#![allow(dead_code)]
// Deprecated: prefer `aifo_coder::docker::image_exists` (re-exported from docker_mod).
//
// Kept temporarily as an empty compatibility module so older codepaths don't break if they
// still reference `util::docker`, but should be removed once confirmed unused.
