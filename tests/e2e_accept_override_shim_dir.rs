#[test]
#[ignore]
fn e2e_accept_phase4_host_override_shim_dir_script_active() {
    // Host override shim dir support was removed (v2+ embedded shims are built into the image).
    eprintln!("skipping: host override shim dir is no longer supported");
}
